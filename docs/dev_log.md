# 개발 로그

## 2026-08-25
- 프로젝트 착수. Flutter 3.44 환경 확인.
- 구현 원칙 확정: 단일 로컬 저장, 데이터 중심 콘텐츠, 세로형 웹/모바일 UI.
- 다음: Flutter 골격, 콘텐츠 JSON, 플레이 가능한 자동 전투 루프 구현.

## 2026-08-25 — 알파 구현
- Flutter 프로젝트와 Provider/SharedPreferences 기반 로컬 저장을 추가했다.
- 자동 전투, 7지역 지도, 7보스 해금, 장비 드랍/장착/분해, 능력치, 무공, 90노드 경맥, 기연, 오프라인 보상, 엔딩을 구현했다.
- 콘텐츠 데이터: 7 지역, 21 일반 적 표기, 7 보스, 24 무공, 20 기연, 90 경맥 노드 정의를 추가했다.
- flutter test, flutter analyze --no-fatal-infos, flutter build web --release를 통과했다.
- 로컬 브라우저 격리 환경이 localhost 접속을 거부하여 화면 자동 점검은 수행하지 못했다. 공개 호스팅 연결 정보는 아직 없다.

## 2026-08-26 — 강호 메인 화면 1차 리디자인
- 자동 전투 계산/저장/드랍 등 기존 Game 로직은 변경하지 않고 강호 메인 전용 UI만 교체했다.
- 캐릭터 상태를 초상·경지·레벨·은자·HP·내력 정보 패널로 재구성했다.
- 지역 진행도, VS 전투 상황, 로그 색상 계층(피해/치명타/회피/획득), 자동전투 상태를 첫 viewport에 배치했다.
- Material NavigationBar를 무협풍 커스텀 하단 탭으로 대체하고, 각 패널의 테두리/색상/타이포그래피 토큰을 일관화했다.
- GitHub Pages 실제 페이지에서 390px 근처 모바일 프레임과 데스크톱 중앙 정렬을 확인했다.

## 2026-08-26 — 무인 화면 1차 리디자인
- 기존 Game 상태와 능력치 투자 동작을 유지한 채 무인 화면 표시 위젯을 교체했다.
- 프로필 패널, 수련 게이지, 능력치 2열 투자 그리드, 전투 능력 요약, 8슬롯 장비 요약을 추가했다.
- 강호 메인과 같은 먹색/한지색/황동 토큰과 각진 서책형 패널을 재사용했다.

## 2026-08-26 — 행낭 화면 1차 리디자인
- 기존 장착/잠금/분해 로직은 유지하고 행낭 전용 UI를 교체했다.
- 슬롯 현황 헤더, 장비 분류 필터, 등급별 색상, 현재 장비 대비 전투 점수 비교를 추가했다.
- 장비 액션을 각 카드 하단에 정리해 모바일에서 장착/잠금/분해 동작을 빠르게 찾을 수 있게 했다.

## 2026-08-26 — 무도 화면 1차 리디자인
- 기존 무공 장착/해제와 경맥 이동 동작을 유지한 채 무도 허브를 교체했다.
- 장착 슬롯 5개, 숙련도, 무공 위력, 보유 무공의 등급/쿨다운/습득 상태를 한 화면에 정리했다.
- 경맥도 진입을 상단 액션으로 통합해 무공과 빌드 화면을 빠르게 오갈 수 있게 했다.
## 2026-08-26 — 강호록 화면 1차 리디자인
- 기존 기록 데이터를 유지한 채 강호록을 서책형 기록 보관소 구조로 교체했다.
- 행적·무공록·병기록·인물록·기연록을 탭으로 분리하고 진행도, 습득 상태, 보스 기록을 한눈에 표시했다.
- 전투/저장 로직은 건드리지 않고 `Game`의 현재 상태만 읽어 기록 화면을 구성했다.
## 2026-08-26 — 기연 이벤트 오버레이 개선
- 기연의 제목, 상황 설명, 선택지 영역을 분리해 읽기 흐름을 개선했다.
- 선택지 번호와 무협 서책형 테두리를 추가했으며 기존 `game.resolve` 결과 처리는 그대로 사용한다.
## 2026-08-26 — 기연 이벤트 화면 1차 리디자인
- 기연 팝업에 현재 지역과 사건 맥락을 추가해 무협 사건 기록의 분위기를 강화했다.
- 각 선택지에 기존 효과 데이터를 읽어 은자·경험치·장비·회복·능력치 보상 힌트를 표시했다.
- 선택 처리와 이벤트 발생 로직은 기존 `Game.resolve` 흐름을 그대로 사용했다.
## 2026-08-26 — 지역 선택 화면 1차 리디자인
- 강호 지도 모달을 카드 대신 밀도 높은 지역 목록으로 재구성했다.
- 지역별 해금 상태, 권장 레벨, 현재 위치, 보스 격파 여부와 진행 막대를 표시한다.
- 기존 `goArea` 호출과 잠금 조건은 변경하지 않았다.
## 2026-08-26 — 지역 상세 / 보스 진입 화면 1차 리디자인
- 강호 메인 지역 패널에 `상세` 진입점을 추가했다.
- 지역 설명, 등장 적, 지역 보스, 해금 안내와 보스 도전 버튼을 바텀시트로 구성했다.
- 기존 보스 해금 조건과 `challenge` 전투 진입 로직은 변경하지 않았다.
## 2026-08-26 — 보스 전투 상태 1차 리디자인
- 보스전 진입 시 전용 경고 패널을 노출해 일반 사냥과의 상태 차이를 즉시 인지할 수 있게 했다.
- 보스명, 다음 지역 해금 목표, 현재 보스 기혈을 전투 영역 상단에 배치했다.
- 자동 전투·보스 계산·보상 로직은 변경하지 않았다.
## 2026-08-26 — 경맥 화면 1차 리디자인
- 경맥도 상단에 개방 노드, 남은 경맥점, 빌드 보너스 요약을 추가했다.
- 개방/개방 가능/잠김 상태 범례를 추가해 90개 노드의 상태를 쉽게 구분하도록 했다.
- 확대·이동·노드 개방·초기화 동작은 기존 `InteractiveViewer`와 `Game` 로직을 유지했다.
## 2026-08-26 — 장비 상세 화면 1차 리디자인
- 행낭 장비 항목을 탭하면 등급, 슬롯, 품질, 공격/방어 옵션과 비교 수치를 확인하는 상세 시트를 연다.
- 장착·잠금·분해 액션을 상세 화면에서도 제공한다.
- 기존 `equip`, `lock`, `breakGear` 로직과 장비 데이터 구조는 변경하지 않았다.
## 2026-08-26 — 오프라인 보상 화면 1차 리디자인
- 객잔 귀환 오버레이를 귀환 보고서 형태로 재구성했다.
- 경과 시간, 지역, 처치 수, 경험치, 은자, 장비·기연 보상 항목을 밀도 있게 배치했다.
- 기존 오프라인 보상 계산과 `claimOffline` 수령 로직은 변경하지 않았다.
## 2026-08-26 — 엔딩 화면 1차 리디자인
- 종장 화면에 무협 서책형 타이틀, 최종 기록 요약, 저장 완료 안내를 추가했다.
- 최종 경지, 격파 수, 개방 지역을 엔딩 통계로 표시한다.
- 엔딩 판정과 `closeEnding` 흐름은 기존 로직을 유지했다.
## 2026-08-26 — 시작 화면 1차 리디자인
- 무협 서책 표지처럼 보이는 로고 프레임과 타이틀 계층을 구성했다.
- 이름 입력과 새 강호 시작 액션을 하나의 시작 패널로 묶어 첫 진입 흐름을 정리했다.
- 기존 `Game.start`와 안내 다이얼로그 동작은 변경하지 않았다.
## 2026-08-26 — 알파 UI 최종 안정화 점검
- 주요 화면 리디자인 범위(강호, 무인, 무도, 행낭, 강호록, 기연, 지역, 보스, 경맥, 오프라인, 엔딩, 시작)를 완료했다.
- `flutter analyze --no-fatal-infos`, `flutter test`, `flutter build web --release`를 모두 통과했다.
- 저장/전투/보스/오프라인 계산 로직을 유지한 상태에서 UI 변경만 누적했으며, 웹 배포 워크플로를 최종 실행했다.
- analyzer에는 기존 스타일 권고(info)만 남아 있고 컴파일 오류는 없다.
## 2026-08-26 — ImageGen 일러스트 자산 1차 적용
- ImageGen으로 주인공 초상, 흑풍채 보스 초상, 청죽림 배경을 생성해 `assets/images/`에 추가했다.
- 강호 상태 패널, 보스전 대결 배지, 청죽림 지역 상세 시트에 일러스트를 연결했다.
- 생성 자산은 기존 UI 위에 보조 비주얼로만 사용했으며 게임 데이터·전투 로직은 변경하지 않았다.
## 2026-08-26 — 후반 지역·기연 일러스트 확장
- ImageGen으로 혈음곡, 천마봉 지역 배경과 산길 기연 장면을 추가했다.
- 지역 상세 화면이 `bamboo`, `blood`, `demon` 지역에 맞는 배경 자산을 자동 선택하도록 연결했다.
- 기연 오버레이에 사건 분위기를 보조하는 일러스트를 적용했다.
## 2026-08-26 — 전 지역 배경 일러스트 확장
- ImageGen으로 낙양 외곽, 흑풍채, 산서대로, 운남 독림 배경을 추가했다.
- 7개 지역 모두 지역 상세에서 전용 배경을 표시하도록 이미지 매핑을 완성했다.
- UI 가독성을 위해 배경은 상세 시트 내부의 제한된 높이로만 노출한다.
## 2026-08-26 — 7개 지역 보스 초상 세트 적용
- ImageGen으로 철면 도적, 청죽 검객, 황토 창왕, 독안 무녀, 혈음 노인, 천마 잔영 초상을 추가했다.
- 기존 흑풍채 보스 초상과 함께 7개 지역 보스가 지역 ID에 맞는 초상을 자동 사용한다.
- 보스 초상은 전투 배지 크기에 맞춰 크롭되며 보스 계산·보상 로직은 변경하지 않았다.
## 2026-08-26 — 장비·무공 아이콘 자산 적용
- ImageGen으로 대표 검 아이콘과 무공 비급 아이콘을 생성했다.
- 무기 장비 카드와 보유 무공 목록에 이미지 아이콘을 연결해 텍스트 중심 화면의 식별성을 높였다.
- 아이콘은 정보 보조용으로만 사용하고 장비·무공 로직은 변경하지 않았다.
## 2026-08-26 — 시작·무인 대표 비주얼 통일
- ImageGen으로 세로형 강호 표지 배경을 생성해 시작 화면에 낮은 불투명도로 적용했다.
- 무인 화면 프로필에도 기존 주인공 초상을 연결해 첫 화면과 캐릭터 정보 화면의 시각 언어를 통일했다.
- 이미지 레이어는 텍스트 가독성을 우선하도록 보조 배경/초상으로만 사용했다.
## 2026-08-26 — 지역 선택 지도 썸네일 적용
- 7개 지역 배경 일러스트를 지역 선택 목록의 썸네일로도 노출했다.
- 잠긴 지역은 썸네일을 낮은 불투명도로 처리하고, 현재/격파 지역은 테두리와 상태 아이콘으로 구분한다.
- 지역 이동 및 잠금 조건 로직은 변경하지 않았다.
## 2026-08-26 — 지역 상세 보스 초상 연결
- 지역 상세의 보스 기록 영역에 지역별 보스 초상과 처치/도전 안내를 추가했다.
- 지도 썸네일, 지역 상세, 전투 배지가 동일한 `areaArtwork`/`bossArtwork` 매핑을 공유한다.
- 보스 도전 버튼과 해금 조건은 기존 동작을 유지했다.
## 2026-08-26 — 강호 전투 영역 지역 배경 적용
- 자동 전투의 VS 전투 상황 패널에 현재 지역 일러스트를 낮은 불투명도로 겹쳐 표시했다.
- 로그와 캐릭터/적 정보의 가독성을 유지하면서 전투 중에도 지역 분위기가 이어지도록 했다.
- 전투 계산과 로그 생성 로직은 변경하지 않았다.
## 2026-08-26 — 전투 대결 배지 주인공 초상 연결
- 자동 전투 VS 영역의 주인공 배지에도 ImageGen 초상을 적용했다.
- 주인공과 적/보스 양쪽의 초상 비율을 통일해 전투 구도를 강화했다.
- 전투 상태와 계산 로직은 변경하지 않았다.
## 2026-08-26 — 일반 적 초상 적용
- ImageGen으로 일반 흑의 산적 제자 초상을 생성했다.
- 보스전이 아닌 자동 전투에서도 적 대결 배지가 전용 초상을 사용하도록 연결했다.
- 적 생성 규칙과 전투 계산은 변경하지 않았다.
## 2026-08-26 — 방어구 아이콘 자산 적용
- ImageGen으로 무협 갑옷 아이콘을 생성했다.
- 무기 외 장비 카드에 갑옷 아이콘을 적용해 행낭 목록의 식별성을 높였다.
- 장비 슬롯·등급·장착 상태 및 장비 로직은 변경하지 않았다.
## 2026-08-26 — 기연 보물 아이콘 적용
- ImageGen으로 옥패 보물 아이콘을 생성했다.
- 기연 선택지의 장비 보상 힌트에 작은 옥패 아이콘을 함께 표시한다.
- 이벤트 결과 처리와 보상 계산은 변경하지 않았다.
## 2026-08-26 — 종장 표지 일러스트 연결
- 시작 화면에 사용한 강호 표지 일러스트를 엔딩 상단에도 낮은 불투명도로 적용했다.
- 시작과 종장이 같은 서책의 앞·뒤 표지처럼 이어지도록 시각적 서사를 강화했다.
- 엔딩 판정과 통계 표시 로직은 변경하지 않았다.
## 2026-08-26 — 경맥 화면 질감 배경 적용
- ImageGen으로 먹색 한지·비단 질감의 경맥 배경을 생성했다.
- 기존 InteractiveViewer와 90노드 그래프 아래에 낮은 불투명도로 배치했다.
- 노드 색상과 텍스트 가독성을 우선해 배경은 보조 레이어로만 사용했다.
## 2026-08-26 — 강호록 서책 질감 적용
- ImageGen으로 낡은 무협 서책 질감 배경을 생성했다.
- 강호록 상단 강호행 기록 패널에 낮은 불투명도로 적용해 기록 보관소 분위기를 강화했다.
- 기록 데이터와 탭 동작은 변경하지 않았다.
## 2026-08-26 — 천마봉 일반 적 변주 초상 적용
- ImageGen으로 천마봉 암영 적 초상을 생성했다.
- 천마봉 일반 전투는 전용 암영 초상을 사용하고, 그 외 지역은 산적 제자 초상을 사용한다.
- 적 생성 규칙과 전투 계산은 변경하지 않았다.
## 2026-08-26 — 운남 독림 일반 적 초상 적용
- ImageGen으로 독사술사 계열 일반 적 초상을 생성했다.
- 운남 독림 일반 전투에서 전용 독사술사 초상을 사용하도록 매핑했다.
- 기존 적 생성 규칙과 전투 계산은 변경하지 않았다.
## 2026-08-26 — 혈음곡 일반 적 초상 적용
- ImageGen으로 혈교도 계열 일반 적 초상을 생성했다.
- 혈음곡 일반 전투에서 전용 혈교도 초상을 사용하도록 연결했다.
- 기존 적 생성·전투 계산 로직은 변경하지 않았다.
## 2026-08-26 — 산서대로 일반 적 초상 적용
- ImageGen으로 황토 교역로의 마적 초상을 생성했다.
- 산서대로 일반 전투에서 전용 마적 초상을 사용하도록 매핑했다.
- 기존 적 생성·전투 계산 로직은 변경하지 않았다.
## 2026-08-26 — 청죽림 일반 적 초상 적용
- ImageGen으로 죽엽승 계열 일반 적 초상을 생성했다.
- 청죽림 일반 전투에서 전용 죽엽승 초상을 사용하도록 연결했다.
- 기존 적 생성·전투 계산 로직은 변경하지 않았다.
## 2026-08-26 — 강호 메인 지역 헤더 아트 연결
- 강호 메인 상단 지역명 패널에도 현재 지역 배경을 낮은 불투명도로 적용했다.
- 지역 선택·상세·전투 헤더가 동일한 지역 아트 언어를 공유하게 됐다.
- UI 정보 계층과 자동 전투 로직은 변경하지 않았다.
## 2026-08-26 — 흑풍채 일반 적 초상 적용
- ImageGen으로 흑풍채 산채 병사 초상을 생성했다.
- 흑풍채 일반 전투에서 전용 산채 병사 초상을 사용하도록 연결했다.
- 기존 전투 로직과 적 생성 규칙은 변경하지 않았다.
## 2026-08-26 — 낙양 외곽 일반 적 초상 적용
- ImageGen으로 산적 견습 초상을 생성했다.
- 낙양 외곽 첫 지역의 일반 전투에서 전용 산적 견습 초상을 사용하도록 연결했다.
- 기존 적 생성·전투 계산 로직은 변경하지 않았다.
## 2026-08-26 — 지역 상세·보스 화면 1차 고도화
- 지역 상세 시트 상단을 지역 일러스트 헤더와 오버레이 제목 구조로 개선했다.
- 지역 보스 정보를 전용 초상 스포트라이트 패널로 확장해 도전/처치 상태를 더 명확히 표시했다.
- 지역 해금, 보스 도전, 보상 및 전투 로직은 변경하지 않았다.
- 390×844 공개 웹 검수에서 하단 탭과 오프라인 보상 흐름을 확인했다.
## 2026-08-26 — 보스 전투 상태 패널 1차 고도화
- 보스 전투 중 상단 상태 패널에 전용 보스 초상과 기혈 게이지를 추가했다.
- 지역 보스전의 위험도와 현재 체력을 강호 메인 첫 화면에서 즉시 파악할 수 있게 했다.
- 보스 전투 계산, 자동전투 토글, 보상 및 해금 로직은 변경하지 않았다.
## 2026-08-26 — 종장 엔딩 연출 1차 고도화
- 최종 보스 초상을 활용한 종장 전용 엔딩 패널을 추가했다.
- 어두운 막과 서책형 프레임, 최종 기록 요약으로 엔딩의 시각적 마무리를 강화했다.
- 엔딩 판정, 저장 및 강호 복귀 동작은 변경하지 않았다.
## 2026-08-26 — 기연 이벤트 화면 1차 고도화
- 기연 이벤트를 어두운 막과 서책형 프레임을 가진 중앙 사건 패널로 개선했다.
- 이벤트 삽화를 확대하고 상황 제목·지역 맥락·선택지 보상 정보를 한 화면에 정돈했다.
- 선택지 결과 처리와 이벤트 데이터 구조는 변경하지 않았다.
## 2026-08-26 — 무인 화면 1차 고도화
- 무인 프로필 패널에 은은한 강호록 질감 배경을 적용했다.
- 장비 슬롯에 무기·방어구 전용 이미지 에셋을 연결해 장착 상태를 빠르게 구분하도록 했다.
- 능력치 투자, 전투력 계산, 장비 장착 로직은 변경하지 않았다.
## 2026-08-26 — 무도 화면 1차 고도화
- 무공 세팅 대표 아이콘과 장착 슬롯에 비급 이미지 에셋을 연결했다.
- 장착 무공과 빈 초식 슬롯의 시각적 대비를 강화했다.
- 무공 숙련도, 자동 발동, 경맥점 및 빌드 계산 로직은 변경하지 않았다.
## 2026-08-26 — 장비 상세 화면 1차 고도화
- 장비 상세 시트 헤더에 무기·방어구 전용 이미지를 연결했다.
- 등급 테두리와 장비 옵션/비교 정보가 실제 아이템 카드처럼 보이도록 시각적 일관성을 높였다.
- 장착, 잠금, 분해 및 장비 비교 로직은 변경하지 않았다.
